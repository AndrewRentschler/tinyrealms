/**
 * @deprecated — We now always use Convex (local or cloud).
 * This file is kept temporarily for any imports that reference it.
 * Will be removed once all references are cleaned up.
 */
export const LOCAL_MODE = false;
