/**
 * AudioManager module. Re-exports types and constants for consumers.
 */

export type { ActiveSfxEntry, SfxHandle } from "./types.ts";
export {
  DEFAULT_AMBIENT_INITIAL_VOLUME,
  DEFAULT_MUSIC_VOLUME,
  DEFAULT_ONE_SHOT_VOLUME,
} from "./constants.ts";
